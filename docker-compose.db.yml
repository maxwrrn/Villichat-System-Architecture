version: '3.8'

services:
  db:
    image: postgres:15
    container_name: local-insight-db
    environment:
      POSTGRES_DB: local_insight_ai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
